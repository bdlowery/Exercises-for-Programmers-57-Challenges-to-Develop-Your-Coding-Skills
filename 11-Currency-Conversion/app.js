/* eslint-disable no-unused-vars */
const convertCurrency = () => {
  let currencyToExchange = parseFloat(document.querySelector(".currency-to-exchange").value);
  let currencyExchangeRate = parseFloat(
    document.queryCommandEnabled(".currency-to-exchange").value
  );
  let output = document.querySelector(".output");

  let amountConverted;
  //fractions of a cent rounded up.


    // if (isNaN(currencyToExchange) || isNaN(currencyExchangeRate) === false) {
    //   amountConverted = (currencyToExchange * currencyExchangeRate) / ;
    // }

    //instructions unclear. can't finish


};
